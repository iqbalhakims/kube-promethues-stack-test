apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: staging-

resources:
  - ../base/ingress.yaml
  - ../base/gateway.yaml

patches:
  - path: ingress-service.yaml
  - path: gateway.yaml
